1. 세트(집합)란 어떠한 자료형의 값이든 각 원소를 '고유하게' 저장하는 객체이다

2. 위크셋은 세트와 유사하지만, 객체만 포함할 수 있다.

3. 맵은 Set과 유사하지만 키/값 쌍으로 이루어진다.
3-1. Set은 for of 루프로만 반복할 수 있지만, Map은 반복을 위해 for fo 루프와 forEach함수 둘 다 사용할 수 있따.

4. 위크맵은 키/값 쌍의 모음이지만, "키는 객체여야만한다" WeakSet과 유사히게 WeakMap에서도 키(객체)는 약하게(얕게)참조된다. 


WeakSet은 객체만 저장할 수 있음.
Map은 키/값 쌍을 저장하고 Set은 값만 저장한다.